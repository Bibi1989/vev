import * as React from "react";

const Drag = () => {
  return (
    <div
      style={{
        width: 100,
        height: 100,
        backgroundColor: "#5392e4",
        justifyContent: "center",
        display: "flex",
        position: "absolute",
        alignItems: "center",
      }}
    >
      Drag me
    </div>
  );
};

export default Drag;
